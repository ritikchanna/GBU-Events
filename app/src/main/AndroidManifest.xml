<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="minor.gbuevents">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Transparent"></activity>
        <activity
            android:name=".submit"
            android:label="Participate"></activity>
        <activity
            android:name=".Abhivyajana"
            android:label="Abhivyanjna" />
        <activity
            android:name=".Shoryautsav"
            android:label="Shoryautsav" />
        <activity
            android:name=".Other"
            android:label="Other Events" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Academic"></activity>
    </application>

</manifest>